import React from 'react'
import { Card, CardContent, Typography, Grid } from '@material-ui/core';

const Tiles = (props) => {
    console.log(props) // checking to see if the data carries over

    return (
        <h2>
            Tiles
        </h2>
    )
}

export default Tiles;